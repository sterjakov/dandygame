#signup.offset
  .content
    .offset


      %h1 Регистрация



      .offsetter

        .px280

          = form_for @user, url: { controller: "auth", action: 'signup' },  html: { style: 'margin: 0 auto;' } do |f|

            .offsetter-height-half

              = render partial: 'block/form_errors', locals: { model: @user }

            .row
              = f.label :email, class: 'required'
              = f.text_field :email

            .row
              = f.label :password, class: 'required'
              = f.password_field :password

            .row
              = f.label :password_confirmation, t('signup.password_confirmation'), class: 'required'
              = f.password_field :password_confirmation


            = render partial: 'block/form_captcha', locals: { f: f }

            .row.actions
              = f.submit '', class: 'hidden'
              = f.hidden_field :vkontakte_id, value: ''
              %a.button.big.orange.all-width{ onclick: 'document.getElementsByTagName("form")[0].submit();' }
                Зарегистрироваться!


            %p.grey.centered Внимание! Поля отмеченные * обязательны для заполенния!








